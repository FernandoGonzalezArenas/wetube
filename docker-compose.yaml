networks:
  teakter-network:
    driver: bridge

services:
  # Base de Datos
  db-global:
    networks:
      - teakter-network
    image: postgres:15
    container_name: postgres-teakter
    environment:
      POSTGRES_USER: ${user_db_env}
      POSTGRES_PASSWORD: ${password_db_env}
    volumes:
      - ./init-scripts/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Consul
  consul:
    networks:
      - teakter-network
    image: hashicorp/consul:1.15
    container_name: consul-server
    ports:
      - "8500:8500"
    command: agent -dev -client=0.0.0.0

  # MinIO
  minio:
    networks:
      - teakter-network
    image: minio/minio
    container_name: minio-server
    environment:
      - MINIO_ROOT_USER=${minio_accessKey_env}
      - MINIO_ROOT_PASSWORD=${minio_secretKey_env}
    ports:
      - "9000:9000"
      - "9001:9001"
    command: server /data --console-address ":9001"

  minio-provisioner:
    image: minio/mc
    depends_on:
      - minio
    networks:
      - teakter-network
    volumes:
      - ./init-scripts/setup-minio.sh:/setup-minio.sh
    entrypoint: /bin/sh
    command: /setup-minio.sh
    environment:
      - MINIO_ROOT_USER=${minio_accessKey_env}
      - MINIO_ROOT_PASSWORD=${minio_secretKey_env}

  rabbitmq:
    networks:
      - teakter-network
    image: rabbitmq:3-management
    container_name: rabbitmq-teakter
    ports:
      - "5672:5672"
      - "15672:15672"

#microservicio auth
  auth-service:
    networks:
      - teakter-network
    build:
      context: ./auth
      dockerfile: Dockerfile
    container_name: auth-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - JWT_SECRET=${jwt_secret_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/authdb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest

  # microservicio video
  video-service:
    networks:
      - teakter-network
    build: 
      context: ./video
      dockerfile: Dockerfile
    container_name: video-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      minio:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - MINIO_ACCESS_KEY=${minio_accessKey_env}
      - MINIO_SECRET_KEY=${minio_secretKey_env}
      - AWS_ACCESS_KEY=${aws_accessKey_env}
      - AWS_SECRET_KEY=${aws_secretKey_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/videodb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - MINIO_URL=http://minio:9000
      - STORAGE_TYPE=minio
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest


  #microservicio comments
  comments-service:
    networks:
      - teakter-network
    build:
      context: ./comments
      dockerfile: Dockerfile
    container_name: comments-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/commentdb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest

#microservicio likes
  likes-service:
    networks:
      - teakter-network
    build:
      context: ./likes
      dockerfile: Dockerfile
    container_name: likes-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/likedb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest

#microservice user
  user-service:
    networks:
      - teakter-network
    build:
      context: ./user
      dockerfile: Dockerfile
    container_name: user-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
      minio:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - MINIO_ACCESS_KEY=${minio_accessKey_env}
      - MINIO_SECRET_KEY=${minio_secretKey_env}
      - AWS_ACCESS_KEY=${aws_accessKey_env}
      - AWS_SECRET_KEY=${aws_secretKey_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/userdb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest
      - MINIO_URL=http://minio:9000
      - STORAGE_TYPE=minio

#microservicio subscription
  subscription-service:
    networks:
      - teakter-network
    build:
      context: ./subscription
      dockerfile: Dockerfile
    container_name: subscription-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/subscriptiondb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest

  #microservicio admin
  admin-service:
    networks:
      - teakter-network
    build:
      context: ./admin
      dockerfile: Dockerfile
    container_name: admin-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/admindb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest

#microservicio notification
  notification-service:
    networks:
      - teakter-network
    build:
      context: ./notification
      dockerfile: Dockerfile
    container_name: notification-microservice
    depends_on:
      db-global:
        condition: service_healthy
      consul:
        condition: service_started
      rabbitmq:
        condition: service_started
    environment:
      - user_db=${user_db_env}
      - password_db=${password_db_env}
      - SPRING_DATASOURCE_URL=jdbc:postgresql://db-global:5432/notificationdb
      - SPRING_CLOUD_CONSUL_HOST=consul
      - RABBIT_HOST=rabbitmq-teakter
      - RABBIT_USER=guest
      - RABBIT_PASSWORD=guest
      - MAIL_HOST=${mail_host_env:-smtp-relay-brevo.com}
      - MAIL_PORT=${mail_port_env:-587}
      - MAIL_USERNAME=${mail_username_env}
      - MAIL_PASSWORD=${mail_password_env}
      - MAIL_FROM=${mail_from_env:-fer.gonare98@gmail.com}
      - FRONTEND_URL=${frontend_url_env:-http://localhost:3000}

  #microservicio gateway
  gateway-service:
    networks:
      - teakter-network
    build:
      context: ./gateway
      dockerfile: Dockerfile
    container_name: gateway-microservice
    ports:
      - "8080:8080"
    depends_on:
      consul:
        condition: service_started
    environment:
      - JWT_SECRET=${jwt_secret_env}
      - SPRING_CLOUD_CONSUL_HOST=consul
      - SPRING_CLOUD_CONSUL_PORT=8500

